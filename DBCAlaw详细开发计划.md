# DB-SafeGuard Enterprise v2.0 详细开发计划
## 前置说明
本计划已根据您的确认信息完成所有细节更新，严格遵循**「自用项目、质量优先、边界刚性、安全第一」**四大原则，所有开发均不修改Hermes Agent核心源码，仅通过继承、钩子、插件、新增文件的方式实现DBA垂直领域定制。

---

## 一、强制执行的核心开发总纲（已确认）
| 原则 | 核心规则 | 刚性技术边界（绝对禁止） |
| :--- | :--- | :--- |
| 自用项目优先 | 作为Hermes分支独立开发，无需同步上游仓库，无需代码review，专注个人使用场景 | 禁止修改Hermes核心源码（`hermes/agent/loop.py`、`hermes/model_provider/`、`hermes/core/`等基础框架文件），禁止破坏Hermes原生能力 |
| 领域边界约束 | 所有功能仅围绕「企业级数据库运维/开发」场景，不做任何通用Agent无关功能 | 禁止开发非DBA领域的通用能力（如浏览器自动化、通用文档处理、代码开发等），禁止超出数据库场景的工具扩展 |
| 安全不可绕过 | 所有数据库操作必须经过「校验→审批→执行→审计」全流程，高危操作必须有刚性拦截 | 禁止任何绕过校验/审批的数据库执行逻辑，禁止硬编码开放无限制的数据库操作权限，禁止明文存储数据库账号密码 |
| 复用优先于重写 | 能复用Hermes原生能力的模块，绝对不重写，仅通过配置/钩子扩展 | 禁止重写Hermes已有的模型调用、记忆存储、工具注册、网关、权限审批等基础能力 |
| 质量与可测性 | 单元测试覆盖率不低于80%，所有操作全链路留痕，审计日志永久留存 | 禁止开发无单元测试的核心功能，禁止开发无日志/无审计的数据库操作逻辑，禁止修改/删除已生成的审计日志 |

---

## 二、分阶段分模块详细开发计划（已确认版）
### 阶段0：项目基线与开发规范定版（前置阶段，周期1周）
**阶段核心目标**：锁定开发规则、环境基线、分支管理策略，从源头避免开发混乱与跑偏。
**无前置依赖**

| 模块名称 | 功能描述 | 技术细节与实现路径 | 刚性技术边界 | 已确认选型/细节 |
| :--- | :--- | :--- | :--- | :--- |
| 代码仓库与分支管理 | 搭建项目仓库，明确分支规则 | 1. Fork Hermes官方仓库，建立私有仓库，保留完整commit历史<br>2. 分支规则：<br> - `main`：稳定运行分支<br> - `dev`：开发主分支<br> - `feature/xxx`：单功能开发分支<br>3. 无需同步上游仓库，无需代码review | 禁止直接修改`main`分支 | ? 无需同步Hermes上游仓库<br>? 无需代码review，自用项目 |
| 开发环境与依赖基线 | 锁定开发环境、Python版本、依赖包版本 | 1. 基础环境：Python 3.11+（与Hermes官方要求一致），uv包管理器<br>2. 核心依赖锁定：<br> - 复用Hermes全量依赖，仅新增DBA专属依赖<br> - 新增依赖：`sqlglot==25.x`、`sqlalchemy==2.x`、`pydantic>=2.0`<br>3. 开发容器化：提供统一开发Docker镜像 | 禁止新增与DBA领域无关的依赖包，禁止修改Hermes官方要求的Python版本与核心依赖版本 | ? 单元测试覆盖率硬性要求：≥80% |
| 编码与文档规范 | 统一编码规范、日志规范、文档规范 | 1. 编码规范：严格遵循Hermes原生PEP8规范，全量类型注解<br>2. 日志规范：复用Hermes日志体系，新增DBA专属日志分类（`dba.audit`、`dba.security`、`dba.execution`）<br>3. 文档规范：每个功能模块必须配套「设计文档+API文档」 | 禁止开发无类型注解的核心代码，禁止开发无对应文档的功能模块 | ? 审计日志留存规则：永久留存 |

---

### 阶段1：Hermes内核适配与基础环境搭建（周期2周）
**阶段核心目标**：跑通Hermes原生内核，完成项目目录结构改造，搭建可二次开发的基础框架。
**前置依赖**：阶段0所有规范确认完成

| 模块名称 | 功能描述 | 技术细节与实现路径 | 刚性技术边界 | 已确认选型/细节 |
| :--- | :--- | :--- | :--- | :--- |
| 项目目录结构改造 | 基于Hermes原生目录，新增DBA专属目录 | 1. 完全保留Hermes原生目录结构，仅新增以下专属目录：<br> - `harnesses/`：规范引擎配置<br> - `library/`：绝对真理RAG知识库<br> - `memory_vault/`：企业级记忆持久化目录<br> - `artifacts/`：SQL脚本、回滚脚本、审计日志输出目录<br>2. 修改`.gitignore`，排除敏感配置、数据库文件<br>3. 合并`pyproject.toml`，新增DBA专属依赖 | 禁止删除/重命名Hermes原生目录，禁止修改Hermes原生项目配置的核心结构 | ? 敏感配置默认放在`~/.hermes-db/`目录，与Hermes原生目录隔离 |
| 基础环境与能力验证 | 完成本地/容器化开发环境搭建，验证Hermes原生核心能力 | 1. 完成依赖安装，跑通`hermes doctor`诊断命令<br>2. 验证Hermes原生能力：模型调用、CLI交互、工具注册、基础记忆功能<br>3. 完成Hermes钩子机制验证：通过自定义Skill、Tool、Pre/Post Hook扩展能力<br>4. 完成Docker镜像构建 | 禁止修改Hermes原生核心命令的基础逻辑，仅可新增DBA专属子命令 | ? 第一期优先适配的大模型：DeepSeek、通义千问、MiniMax、Kimi、GLM、Gemini、GPT |
| 扩展点预留 | 为后续DBA专属功能预留扩展入口 | 1. 新增`hermes/extensions/dba/`目录，作为所有DBA专属代码的唯一存放路径<br>2. 预留Hermes Agent Loop的生命周期钩子：`pre_task`、`post_tool_call`、`pre_execution`、`post_task`<br>3. 预留配置扩展项，在Hermes原生配置中新增`dba`专属配置块 | 所有DBA专属代码必须放在`hermes/extensions/dba/`目录下，禁止散落在Hermes原生目录中 | ? 配置文件格式：复用Hermes原生yaml配置，新增`dba`配置块 |

---

### 阶段2：核心工具层与安全沙箱开发（周期3周）
**阶段核心目标**：完成DBA专属工具集封装，搭建零信任安全沙箱，这是所有上层功能的基础。
**前置依赖**：阶段1完成，基础环境验证通过

| 模块名称 | 功能描述 | 技术细节与实现路径 | 刚性技术边界 | 已确认选型/细节 |
| :--- | :--- | :--- | :--- | :--- |
| 数据库安全连接适配器 | 实现读写分离的数据库连接池，统一管理多数据库连接 | 1. 基于SQLAlchemy 2.x封装，适配多数据库方言，复用Hermes的配置管理与加密能力<br>2. 核心能力：<br> - 读写分离：只读连接/管理员连接双实例<br> - 连接隔离：每个会话/任务使用独立连接<br> - 密码加密：复用Hermes的密钥管理能力<br> - 超时控制：默认30s超时<br>3. 适配数据库：Oracle、MySQL、PostgreSQL、SQL Server | 1. 禁止默认实例化管理员权限连接<br>2. 禁止在日志中输出数据库账号密码、敏感数据<br>3. 仅支持在`library/`目录中有对应规范的数据库 | ? 第一期优先支持的数据库：Oracle、MySQL、PostgreSQL、SQL Server<br>? 数据库连接默认超时时间：30s |
| AST语法解析工具 | 离线SQL语法校验、方言适配、风险等级识别 | 1. 基于sqlglot封装，完全兼容Hermes的工具注册体系<br>2. 核心能力：<br> - 语法正确性校验<br> - 方言适配校验<br> - 操作类型识别，映射L0-L4风险等级<br> - 语法树解析<br>3. 输出标准化：统一返回「校验结果+错误详情+修正建议+风险等级」结构化数据 | 1. 仅做语法层面校验，不做执行计划/性能校验<br>2. 禁止修改原始SQL，仅做解析与校验<br>3. 禁止绕过语法校验直接进入下一环节 | ? 语法校验失败后，AI自动重写最大轮次：3轮 |
| 执行计划校验工具 | 线上SQL性能风险识别，拉取EXPLAIN执行计划 | 1. 基于数据库原生EXPLAIN能力封装，使用只读连接执行<br>2. 核心能力：<br> - 执行计划自动拉取<br> - 性能风险识别：全表扫描、无索引JOIN、超阈值扫描行数<br> - 扫描行数阈值告警：默认1000行<br> - 优化建议生成<br>3. 输出标准化：统一返回「执行计划详情+风险点+优化建议+是否拦截」结构化数据 | 1. 仅使用只读连接执行EXPLAIN，绝对不修改任何数据<br>2. 仅做性能风险识别，不做语法校验 | ? 执行计划校验的默认扫描行数阈值：1000行<br>? 不强制要求配置备库才能使用 |
| 元数据读取工具 | 安全读取库表结构、字段属性、索引信息 | 1. 基于只读连接封装，严格限制仅可读取系统表/信息_schema<br>2. 核心能力：<br> - 库表列表读取<br> - 表结构DDL读取<br> - 索引信息读取<br> - 字段属性、类型、注释读取<br>3. 输出标准化：统一返回Markdown格式的表结构信息 | 1. 仅使用只读连接，禁止任何写操作<br>2. 禁止读取表内的业务数据，仅可读取元数据 | ? 元数据缓存策略：缓存表结构信息，缓存有效期1小时 |
| 回滚脚本生成工具 | 针对DML/DDL操作自动生成对应回滚脚本 | 1. 基于SQL语法树与元数据信息生成，适配不同数据库的回滚逻辑<br>2. 核心能力：<br> - DML操作：生成反向INSERT/UPDATE/DELETE脚本<br> - DDL操作：生成反向表结构修改脚本<br> - 脚本校验：验证回滚脚本的语法正确性<br>3. 输出标准化：返回可直接执行的回滚SQL脚本+执行说明 | 1. 仅生成回滚脚本，不自动执行<br>2. 禁止生成无校验的回滚脚本 | ? 强制要求L2及以上风险操作必须生成回滚脚本才能进入审批环节 |
| 审计日志工具 | 全链路操作记录，所有数据库操作全程留痕 | 1. 复用Hermes的日志体系，新增专属审计日志分类，持久化到本地文件+SQLite数据库<br>2. 核心能力：<br> - 全字段记录：操作人、时间、会话ID、数据库实例、原始SQL、风险等级、校验结果、审批状态、执行结果、耗时<br> - 检索能力：支持按时间、用户、风险等级、数据库、操作类型检索<br> - 导出能力：支持批量导出Excel/CSV格式的审计日志 | 1. 禁止修改/删除已生成的审计日志<br>2. 禁止遗漏任何数据库操作的审计记录 | ? 审计日志永久留存<br>? 支持批量导出 |

---

### 阶段3：Harness规范引擎与意图路由开发（周期2周）
**阶段核心目标**：实现从源头约束AI行为的规范引擎，为AI戴上“操作手册”。
**前置依赖**：阶段2工具层开发完成，单元测试通过

| 模块名称 | 功能描述 | 技术细节与实现路径 | 刚性技术边界 | 已确认选型/细节 |
| :--- | :--- | :--- | :--- | :--- |
| 绝对真理RAG知识库 | 数据库官方规范文档库，从源头解决AI方言混淆、语法幻觉问题 | 1. 基于Hermes原生Context Files与RAG检索工具改造，严格限制检索范围<br>2. 核心实现：<br> - 物理目录隔离：`library/[数据库类型]/[版本]/`，存放切分后的官方MD格式文档<br> - 强制检索规则：根据目标数据库类型+版本，锁定检索路径<br> - 检索触发机制：SQL生成、语法校验环节必须触发知识库检索<br>3. 文档切片规则：按功能模块切片，单切片不超过1000token | 1. 禁止AI检索超出当前目标数据库范围的文档<br>2. 禁止无官方文档支撑的SQL语法输出<br>3. 禁止修改`library/`目录下的官方文档 | ? 文档切片的最大token数：1000<br>? 支持用户自定义上传企业内部规范文档 |
| 意图分类与路由引擎 | 精准识别用户需求的类型、目标数据库、风险等级，自动挂载对应规范 | 1. 基于Hermes的Skill触发规则+分类Prompt实现，复用Hermes的LLM调用能力<br>2. 核心能力：<br> - 意图分类：识别需求类型（查询/DDL/DML/调优/备份/故障排查/巡检）<br> - 目标识别：识别目标数据库类型、实例、版本<br> - 风险预分级：预判操作风险等级<br> - 规范自动挂载：根据意图与目标数据库，自动读取对应Harness规范<br>3. 分类准确率保障：内置分类示例库，少样本Prompt提升分类准确率 | 1. 仅做意图分类与规范挂载，不做任务执行<br>2. 禁止无规范挂载的任务进入执行环节 | ? 支持人工修正意图分类，修正结果自动沉淀到分类示例库 |
| Harness规范管理体系 | 企业运维红线、工作流规范、风险规则的统一管理 | 1. 基于Markdown/YAML文件实现，无需硬编码，修改后自动生效<br>2. 核心规范文件：<br> - `harnesses/contracts/hitl_matrix.yaml`：L0-L4风险矩阵、五档模式放行规则、用户权限映射<br> - `harnesses/stages/[场景].md`：不同场景的工作流规范<br> - `harnesses/taxonomy/intent_classification.yaml`：意图分类规则与示例<br>3. 规范加载机制：任务启动时自动加载对应规范，支持运行时热重载 | 1. 所有规范必须以外挂文件形式存在，禁止硬编码到代码中<br>2. 禁止修改规范后需要重启服务才能生效 | ? 规范文件支持权限管控，仅管理员可修改 |

---

### 阶段4：闭环执行引擎与多智能体流水线开发（周期3周）
**阶段核心目标**：实现DBA专属的闭环执行流水线，基于Hermes子代理架构，实现“生成→校验→驳回→优化→验收”的闭环。
**前置依赖**：阶段3规范引擎开发完成，阶段2工具层验证通过

| 模块名称 | 功能描述 | 技术细节与实现路径 | 刚性技术边界 | 已确认选型/细节 |
| :--- | :--- | :--- | :--- | :--- |
| DBA专属主Agent循环 | 基于Hermes Agent Loop改造的数据库专属执行主循环 | 1. 继承Hermes原生Agent Loop类，通过重写生命周期钩子实现定制，不修改核心源码<br>2. 核心流程：<br> `需求接收→意图分类→规范挂载→元数据预检→方案生成→双Agent并行校验→风险定级→人工审批（按需）→安全执行→结果反馈→经验沉淀` <br>3. 核心能力：<br> - 任务中断与重试：支持手动中断、失败自动重试（最多2次）<br> - 断点续跑：任务异常中断后，可从断点继续执行<br> - 状态管理：全流程状态追踪，实时推送状态事件到前端 | 1. 必须严格遵循预设的工作流规范，禁止跳过任何校验/审批环节<br>2. 禁止修改Hermes原生Agent Loop的核心调度逻辑<br>3. 校验不通过的任务，必须打回重写 | ? 任务失败后的最大自动重试次数：2次 |
| 双校验子Agent流水线 | 基于Hermes Subagent子代理架构，实现语法校验Agent+性能审核Agent双独立校验 | 1. 复用Hermes原生Subagent能力，每个子代理拥有独立上下文、受限工具集，**并行执行**<br>2. 子代理职责：<br> - 语法校验Agent：仅可调用AST语法解析工具、RAG知识库，负责语法正确性、方言兼容性校验，校验不通过直接打回<br> - 性能审核Agent：仅可调用执行计划校验工具、元数据读取工具，负责性能风险识别，识别到高危风险直接打回<br>3. 一票否决机制：任意一个子代理校验不通过，任务直接打回主Agent重写 | 1. 两个子代理必须完全独立，上下文隔离，工具集严格受限<br>2. 子代理仅可做校验与建议，禁止执行任何数据库写操作<br>3. 禁止绕过子代理校验直接进入审批环节 | ? 两个子代理并行执行 |
| 任务预检模块 | 任务执行前的前置检查，提前获取必要的元数据 | 1. 作为主Agent循环的pre_task钩子实现<br>2. 核心检查项：<br> - 数据库连接可用性检查<br> - 目标库表是否存在<br> - 当前连接的权限是否满足任务需求<br> - 元数据预读取，注入AI上下文<br>3. 预检不通过的任务，直接终止，给出明确错误原因 | 1. 仅做前置检查，不执行任何SQL操作<br>2. 预检不通过的任务，禁止进入方案生成环节 | ? 任务预检默认开启，不允许用户关闭 |

---

### 阶段5：企业级多级记忆中枢开发（周期3周）
**阶段核心目标**：100%兼容Hermes四层记忆架构，针对DBA领域深度定制，实现企业级业务经验沉淀与复用。
**前置依赖**：阶段4执行引擎核心流程跑通

| 模块名称 | 功能描述 | 技术细节与实现路径 | 刚性技术边界 | 已确认选型/细节 |
| :--- | :--- | :--- | :--- | :--- |
| L1 工作区记忆适配 | 当前会话的临时上下文管理，严格控制长度 | 1. 完全复用Hermes原生提示词记忆体系，仅扩展DBA专属上下文字段<br>2. 核心存储内容：<br> - 当前工单需求、用户要求<br> - 目标库表结构、元数据信息<br> - 历史重试草稿、校验记录<br> - 当前任务状态、风险等级<br>3. 长度控制：自动压缩无效内容，总长度不超过4000token | 1. 仅存储当前会话的临时上下文，会话结束后自动归档到L2记忆<br>2. 禁止无限制的上下文堆积 | ? L1工作区记忆的最大token阈值：4000 |
| L2 情景会话记忆适配 | 全量历史会话记录，支持跨会话检索与回溯 | 1. 100%复用Hermes原生SQLite+FTS5全文检索体系，无需重写<br>2. 扩展能力：<br> - 新增DBA专属标签：按数据库类型、操作类型、风险等级给会话打标签<br> - 扩展检索能力：支持按标签、数据库实例、时间范围精准检索<br>3. 存储内容：全量会话历史、操作日志、校验记录、执行结果 | 1. 禁止修改Hermes原生会话存储的核心结构，仅可新增扩展字段<br>2. 禁止删除历史会话记录，仅支持逻辑删除 | ? 历史会话自动归档周期：30天 |
| L3 运维经验记忆开发 | 沉淀历史成功SQL范式、故障排查方案、避坑指南 | 1. 基于Hermes原生程序性记忆（技能库）+**Hermes原生SQLite向量扩展**实现，复用Hermes的记忆归档逻辑<br>2. 核心实现：<br> - 自动沉淀：任务成功完成后，自动触发经验提炼，生成结构化Markdown文件<br> - 向量化存储：使用Hermes原生SQLite向量扩展做向量化索引<br> - 智能召回：同类任务启动时，自动匹配历史经验，注入AI上下文<br> - **人工确认**：经验沉淀必须经过用户确认才能入库<br>3. 管理能力：支持经验的人工编辑、启用/禁用、版本管理 | 1. 仅沉淀成功完成的任务经验，失败任务不自动沉淀<br>2. 经验沉淀必须经过用户确认，禁止未经确认的经验自动入库<br>3. 禁止修改已归档的经验版本，仅可新增版本 | ? 向量数据库选型：复用Hermes原生SQLite向量扩展<br>? 运维经验沉淀需要默认开启人工确认 |
| L4 业务图谱与用户建模开发 | 维护企业业务血缘关系、业务字典、用户操作习惯 | 1. 基于Hermes原生Honcho用户建模体系扩展，新增业务图谱专属存储模块<br>2. 核心内容：<br> - 业务血缘字典：跨表字段映射关系、表关联关系<br> - 业务状态码释义：业务字段的枚举值、状态含义<br> - 业务规则：企业专属的业务逻辑、数据规范<br> - 用户建模：用户的操作习惯、偏好的SQL风格、风险模式选择<br>3. 加载机制：所有对话前预加载，全程生效 | 1. 业务图谱的修改必须经过管理员审批，禁止AI自动修改<br>2. 禁止AI使用业务图谱之外的业务逻辑做判断<br>3. 禁止存储用户的敏感信息、密码等内容 | ? 支持从企业数据字典手动导入业务图谱 |

---

### 阶段6：自进化DBA技能体系开发（周期2周）
**阶段核心目标**：100%复用Hermes自主技能生成体系，定制DBA专属技能库。
**前置依赖**：阶段5记忆中枢开发完成

| 模块名称 | 功能描述 | 技术细节与实现路径 | 刚性技术边界 | 已确认选型/细节 |
| :--- | :--- | :--- | :--- | :--- |
| DBA内置技能库开发 | 数据库运维全场景内置技能，开箱即用 | 1. 基于Hermes原生Skill格式开发，完全兼容Hermes的技能注册、调用、管理体系<br>2. 第一期内置技能清单（按优先级）：<br> - SQL安全生成与优化技能<br> - DDL变更全流程执行技能<br> - 数据库健康巡检技能<br> - 索引优化建议技能<br> - 故障排查根因分析技能<br>3. 技能格式：严格遵循Hermes原生SKILL.md格式 | 1. 所有内置技能必须严格遵循安全规则，禁止无校验的执行逻辑<br>2. 技能仅可调用已注册的DBA专属工具 | ? 第一期优先开发的内置技能：SQL安全生成、DDL变更、健康巡检、索引优化、故障排查 |
| 自主技能生成机制 | 从成功的复杂任务中自动生成可复用的结构化技能 | 1. 100%复用Hermes原生技能生成逻辑，仅定制DBA专属技能模板与触发条件<br>2. 触发条件（满足任一即可）：<br> - 同类复杂任务执行成功超过2次<br> - 执行中报错但完成自我修复<br> - 被用户纠正并优化了方案<br> - 发现了高效的运维解决方案<br>3. 技能生成流程：<br> 任务完成→经验评估→技能结构化生成→**用户确认**→技能入库→版本管理 | 1. 技能生成必须经过用户确认，禁止未经确认的技能自动入库启用<br>2. 禁止生成超出DBA领域的技能<br>3. 技能更新必须采用patch方式，禁止全量重写历史版本 | ? 技能生成需要默认开启人工确认 |
| 技能自我优化机制 | 技能使用过程中，发现更优方案时自动更新优化 | 1. 复用Hermes原生技能自我修复逻辑，定制DBA场景的优化规则<br>2. 核心实现：<br> - 技能执行后自动评估效果，对比历史执行结果<br> - 发现更优方案时，生成技能优化patch<br> - 经过**用户确认**后，更新技能版本<br>3. 版本管理：保留所有历史版本，支持一键回滚 | 1. 技能优化必须经过用户确认，禁止自动修改已启用的技能<br>2. 禁止无版本管理的技能修改 | ? 技能优化需要开启人工确认 |
| 技能库管理能力 | 技能的启用/禁用、版本管理、权限管控 | 1. 复用Hermes原生技能管理体系，扩展企业级权限管控<br>2. 核心能力：<br> - 技能的启用/禁用开关<br> - 版本管理与回滚<br> - 按用户角色分配技能使用权限<br>3. 权限映射：<br> - 管理员：所有技能可用<br> - 开发者用户：L0-L3相关技能可用<br> - 普通用户：仅查询类技能可用 | 1. 禁止普通用户修改内置技能，仅管理员可修改<br>2. 禁用的技能禁止被AI调用 | ? 用户权限管理：管理员（权限最大）、普通用户（只读）、开发者用户（可以使用L0-L3） |

---

### 阶段7：零信任安全管控与五档人机协同开发（周期2周）
**阶段核心目标**：实现L0-L4风险分级管控与五档人机协同模式，兼顾效率与生产安全。
**前置依赖**：阶段4执行引擎开发完成，阶段2工具层的风险识别能力验证通过

| 模块名称 | 功能描述 | 技术细节与实现路径 | 刚性技术边界 | 已确认选型/细节 |
| :--- | :--- | :--- | :--- | :--- |
| L0-L4风险分级体系 | 标准化的SQL操作风险分级规则 | 1. 基于`harnesses/contracts/hitl_matrix.yaml`配置实现，零代码可修改<br>2. 风险分级标准（硬编码兜底规则）：<br> - L0 无损只读：SELECT查询、元数据读取、健康诊断<br> - L1 低危单行DML：单行数据增删改、无锁索引创建<br> - L2 中危结构扩展：批量数据操作、表字段新增/修改<br> - L3 高危结构破坏：表结构删除/修改、数据清理、TRUNCATE操作<br> - L4 灾难级操作：全库操作、核心表删除、DROP DATABASE操作<br>3. 风险识别：基于AST语法解析结果，自动匹配风险等级 | 1. 风险分级必须严格遵循配置规则，禁止AI自行调整风险等级<br>2. 禁止高风险操作低定级，兜底规则硬编码拦截 | ? 支持用户自定义风险分级规则 |
| 五档人机协同模式 | 前端可动态切换的安全模式，适配不同环境与场景 | 1. 基于Hermes原生Tool Approval机制扩展，修改审批判断逻辑<br>2. 五档模式规则：<br> - 极度保守模式：仅自动放行L0，任何写操作强制人工审批<br> - 适度模式（默认）：放行L0/L1，L2及以上强制人工确认并审核回滚脚本<br> - 激进模式（开发/测试）：放行L0/L1/L2，仅拦截L3/L4破坏性操作<br> - 疯子模式：全量放行L0-L4，无阻断自动执行，**需要管理员密码才能开启**<br> - 只读审计模式：强制锁定底层为只读连接，纯诊断无写入权限<br>3. 模式切换：前端可一键切换，管理员可锁定模式 | 1. 只读审计模式下，必须强制锁定所有连接为只读，禁止任何写操作<br>2. 除疯子模式外，所有模式必须严格遵循放行规则<br>3. 疯子模式不禁用，但需要管理员密码才能开启 | ? 生产环境不需要默认禁用疯子模式，但开启需要管理员密码 |
| 人工审批流程 | 高危操作的人工审批、挂起、驳回、授权流程 | 1. 基于Hermes原生审批机制扩展，新增SSE事件推送<br>2. 核心能力：<br> - 审批触发：风险等级与当前模式不匹配时，自动挂起任务，触发审批<br> - 审批信息推送：实时推送审批事件到前端，包含SQL内容、风险等级、校验结果、回滚脚本<br> - 审批操作：支持通过/驳回/修改后执行，驳回需填写原因<br> - **单人审批**，无超时设置<br>3. 审计：所有审批操作全量记录到审计日志 | 1. 未通过审批的任务，绝对禁止执行<br>2. 禁止审批环节修改原始SQL的风险等级<br>3. 审批记录必须永久留存，不可篡改 | ? 审批流程：仅单人审批，暂时不用审批超时时间 |

---

### 阶段8：前端观测大盘与UI开发（周期3周）
**阶段核心目标**：实现白盒化可观测的前端交互界面，全链路可视化AI思考与执行过程。
**前置依赖**：阶段4执行引擎的SSE事件推送机制开发完成

| 模块名称 | 功能描述 | 技术细节与实现路径 | 刚性技术边界 | 已确认选型/细节 |
| :--- | :--- | :--- | :--- | :--- |
| 前端技术栈选型与基础框架搭建 | 基于Hermes原生Web UI改造，搭建基础项目框架 | 1. **基于Hermes原生Web UI（React+TypeScript）改造**，仅修改页面与组件，复用底层通信、状态管理能力<br>2. 核心基础能力：<br> - 与后端的WebSocket/SSE通信<br> - 用户权限管理与登录<br> - 响应式布局，适配PC端<br> - 暗黑/明亮主题切换 | 1. 前端必须严格遵循后端的安全规则，禁止绕过审批执行操作<br>2. 前端仅做展示与交互，所有业务逻辑必须在后端实现 | ? 前端开发方案：基于Hermes原生Web UI改造 |
| 对话交互主页面 | 核心操作页面，用户与AI交互、发起任务、查看执行过程 | 1. **左中右三栏布局**：<br> - 左栏：会话列表、数据库实例切换、模式切换<br> - 中间栏：对话窗口，上下分栏：<br>   上半部分：用户提问与AI回复，支持Markdown、SQL代码高亮、格式化、一键复制<br>   下半部分：**思考流/校验过程，不默认展开，但可以手动展开**，流式展示AI的检索、校验、推理全链路<br> - 右栏：当前会话上下文，包含库表信息、风险等级、执行状态、审批进度<br>2. 核心功能：<br> - 对话发起、中断、重试、撤销<br> - SQL代码高亮、格式化、一键复制、一键执行<br> - 风险等级高亮提示，审批弹窗 | 1. 对话窗口必须完整展示全链路执行过程，禁止隐藏校验/风险信息<br>2. 高危操作必须有二次确认弹窗，禁止一键执行<br>3. SQL代码必须支持高亮、格式化、一键复制 | ? 主对话页面采用左中右三栏布局<br>? 思考流/校验过程不默认展开，但可以手动展开<br>? 支持SQL代码在线格式化、语法高亮、一键复制 |
| 运维观测大盘页面 | 总览面板，展示全平台执行统计、风险事件、成功率、健康状态 | 1. 核心模块：<br> - 数据概览：总执行次数、成功率、风险事件数、活跃会话数<br> - 风险统计：按风险等级统计的操作分布、拦截次数<br> - 执行趋势：近7天/30天的执行趋势图<br> - 高危操作预警：待审批的高危操作、最近拦截的风险事件<br> - 常用技能快速入口<br>2. 权限控制：仅管理员可查看全平台数据，普通用户仅可查看自己的数据<br>3. **不支持自定义看板** | 1. 必须严格遵循权限控制，禁止普通用户查看其他用户的数据<br>2. 风险事件必须高亮预警，禁止隐藏 | ? 观测大盘不需要支持自定义看板 |
| 审计日志页面 | 全量操作记录的查询、筛选、导出 | 1. 核心功能：<br> - 多条件筛选：按时间、用户、数据库实例、风险等级、操作类型、执行状态筛选<br> - 详情查看：点击单条日志可查看完整执行详情、SQL内容、审批记录<br> - **批量导出功能**：支持批量导出Excel/CSV格式的审计日志<br>2. 权限控制：仅管理员可查看全量审计日志，普通用户仅可查看自己的操作日志 | 1. 禁止修改/删除审计日志，仅可查看与导出<br>2. 必须严格遵循权限控制，禁止越权查看 | ? 审计日志需要支持批量导出 |
| 管理配置页面 | 系统配置、规则管理、数据库连接管理、用户权限管理 | 1. 子页面拆分：<br> - 数据库连接管理：新增/编辑/删除数据库实例，测试连接<br> - 规则与规范管理：风险矩阵、工作流规范、意图分类规则配置<br> - 知识库管理：官方文档、企业内部规范的上传/管理<br> - 记忆与技能管理：运维经验、业务图谱、技能库的管理<br> - 用户与权限管理：用户新增/编辑/删除、角色分配（管理员/普通用户/开发者用户）<br> - 系统设置：模型配置、安全配置、日志配置、备份策略配置<br>2. 权限控制：仅管理员可访问管理配置页面 | 1. 仅管理员可访问管理配置页面<br>2. 数据库密码必须加密存储，禁止明文展示在前端<br>3. 配置修改必须记录审计日志 | ? 用户权限管理：管理员（权限最大）、普通用户（只读）、开发者用户（可以使用L0-L3） |

---

### 阶段9：企业级集成与部署适配开发（周期2周）
**阶段核心目标**：实现企业级系统集成、多环境部署适配，满足企业内网私有化部署需求。
**前置依赖**：阶段1-8核心功能开发完成，单元测试通过

| 模块名称 | 功能描述 | 技术细节与实现路径 | 刚性技术边界 | 已确认选型/细节 |
| :--- | :--- | :--- | :--- | :--- |
| 企业级SSO单点登录适配 | 对接企业现有身份体系，实现单点登录 | 1. 基于OAuth2.0/OIDC协议实现，支持企业微信、钉钉<br>2. 核心能力：<br> - SSO登录/登出<br> - 用户角色自动映射（管理员/普通用户/开发者用户）<br> - 权限自动同步<br>3. 适配方式：插件化实现，可配置启用/禁用，支持关闭SSO使用本地账号登录 | 1. 禁止硬编码SSO配置，所有配置可通过环境变量/配置文件修改<br>2. 必须支持关闭SSO，使用本地账号登录 | ? 第一期优先适配的SSO平台：企业微信/钉钉 |
| 第三方系统集成Webhook | 对接企业现有工单系统、监控系统 | 1. 基于Hermes原生Gateway网关扩展，新增Webhook适配器<br>2. 核心能力：<br> - 飞书工单Webhook：接收飞书工单的数据库变更请求，自动执行流程，同步执行结果<br> - 企业微信告警接收：接收企业微信的数据库告警，自动触发故障排查技能<br>3. 适配方式：插件化实现，可配置启用/禁用 | 1. Webhook必须做鉴权，禁止无鉴权的请求触发任务<br>2. 第三方系统仅可触发预设的技能，禁止执行任意操作 | ? 第一期优先对接的第三方系统：飞书工单/企业微信告警 |
| 多环境部署方案 | 适配不同部署场景，提供开箱即用的部署方案 | 1. **第一期优先提供Docker单机部署方案**，docker compose一键启动<br>2. 核心能力：<br> - Docker单机部署：推荐生产环境使用，docker compose一键启动<br> - 数据持久化：配置、记忆、技能、审计日志全量持久化到本地卷<br>3. 部署包：提供一键部署脚本、Docker镜像、docker-compose.yml | 1. 所有部署方案必须支持私有化部署，数据100%留存本地<br>2. 禁止部署包包含任何外部数据回传逻辑<br>3. 必须支持离线部署，无需公网访问 | ? 第一期优先提供的部署方案：Docker单机 |
| 数据备份与恢复 | 系统数据的备份、恢复、迁移能力 | 1. 核心能力：<br> - **轻量备份策略**：仅备份配置、记忆、技能、审计日志，不备份大明细数据<br> - 自动备份：支持设置自动备份策略（周期可配置）<br> - 一键恢复：备份文件一键恢复<br> - 版本迁移：支持跨版本数据迁移<br>2. 备份文件支持加密存储 | 1. 备份文件必须支持加密存储<br>2. 恢复操作必须有二次确认，禁止覆盖数据无提示 | ? 自动备份策略：轻量备份，仅备份配置、记忆、技能、审计日志，支持设置自动备份周期 |

---

### 阶段10：测试、优化与完善（周期2周，无发布时间计划）
**阶段核心目标**：完成全场景测试、性能优化、文档完善，确保代码质量高，开发成功。
**前置依赖**：阶段1-9所有功能开发完成

| 模块名称 | 功能描述 | 技术细节与实现路径 | 刚性技术边界 | 已确认选型/细节 |
| :--- | :--- | :--- | :--- | :--- |
| 全场景功能测试 | 覆盖所有功能模块的单元测试、集成测试、场景测试 | 1. 单元测试：覆盖所有核心工具、函数、模块，**覆盖率不低于80%**<br>2. 集成测试：覆盖全流程链路，验证模块间的交互正常<br>3. 场景测试：覆盖核心用户场景，模拟真实使用环境<br>4. 安全测试：权限越权测试、SQL注入测试、高危操作拦截测试 | 1. 核心功能测试不通过，禁止投入使用<br>2. 安全测试发现高危漏洞，必须修复后才能使用 | ? 单元测试覆盖率硬性要求：≥80%<br>? 不需要第三方安全渗透测试 |
| 性能与稳定性优化 | 优化系统性能、降低资源占用、提升稳定性 | 1. 核心优化项：<br> - 向量检索性能优化<br> - 大会话上下文压缩优化<br> - 数据库连接池性能优化<br> - 内存占用优化<br>2. 压测：模拟多用户并发场景，验证系统稳定性 | 1. 禁止为了性能优化突破安全规则<br>2. 优化后必须重新进行全量测试 | ? 并发用户数压测目标：50人 |
| 文档完善 | 完善全量文档，确保代码可维护、可理解 | 1. 文档清单：<br> - 快速开始手册<br> - 用户操作手册<br> - 管理员运维手册<br> - 开发手册（二次开发指南）<br> - 部署手册<br> - 常见问题FAQ | 1. 文档必须与代码功能完全同步，禁止出现文档与功能不一致的情况 | ? 不需要提供配套的视频教程 |

---

## 三、最终确认的核心细节汇总
### 1. 基础规则
- ? 无需同步Hermes上游仓库
- ? 无需代码review，自用项目
- ? 单元测试覆盖率≥80%
- ? 审计日志永久留存

### 2. 技术选型
- ? 向量数据库：复用Hermes原生SQLite向量扩展
- ? 第一期数据库：Oracle、MySQL、PostgreSQL、SQL Server
- ? 第一期模型：DeepSeek、通义千问、MiniMax、Kimi、GLM、Gemini、GPT
- ? 前端：基于Hermes原生Web UI改造

### 3. 功能边界
- ? 语法校验失败后，AI自动重写最大3轮
- ? 执行计划阈值1000行，不强制备库
- ? L2及以上必须生成回滚脚本
- ? 运维经验、技能生成/优化需要人工确认
- ? 疯子模式不禁用，但需要管理员密码
- ? 单人审批，无超时
- ? 用户权限：管理员、普通用户(只读)、开发者用户(L0-L3)

### 4. 前端与交互
- ? 左中右三栏布局
- ? 思考流/校验过程不默认展开，可手动展开
- ? 观测大盘不自定义
- ? 审计日志批量导出

### 5. 企业级集成
- ? SSO：企业微信/钉钉
- ? 第三方：飞书工单/企业微信告警
- ? 部署：Docker单机
- ? 备份：轻量策略，仅备份配置、记忆、技能、审计日志

### 6. 发布与测试
- ? 不需要第三方渗透测试
- ? 不需要视频教程
- ? 无发布时间计划，个人开发，重质量

---